# Dota 2 联动

1. 打开适配器目录，复制 `gamestate_integration_yokonex.cfg`。
2. 放到 `dota 2 beta/game/dota/cfg/gamestate_integration/`。
3. 重启 Dota 2，在 GameHub 中启用联动并保存指令 ID。

这是 Dota 2 自带的 GSI 接口，不依赖第三方平台。

默认 commandId 与原项目 `event_configs.json` 完全一致。
