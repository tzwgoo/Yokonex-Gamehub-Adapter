# 英雄联盟联动

无需安装插件。进入对局后，Yokonex GameHub 会通过游戏自带的 Live Client Data 接口自动读取本地玩家状态。

1. 在“游戏联动”中启用“英雄联盟”。
2. 为每个事件填写指令 ID 并保存。
3. 保持 GameHub 运行，然后进入英雄联盟对局。

默认 commandId 与原 `ycy-lol-link` 一致，使用 `0` 到 `4`；多个事件可以复用同一个 commandId。

训练模式和正常对局均可测试。客户端大厅阶段不会产生事件。
