# 饥荒联机版联动

1. 将本目录中的 `modinfo.lua` 和 `modmain.lua` 复制到游戏的 `mods/yokonex_gamehub_link/`。
2. 在游戏 Mod 页面启用 `Yokonex GameHub Link`。
3. 在 GameHub 中启用“饥荒联机版”并保存指令 ID。

Mod 只写本地 `client_log.txt`，GameHub 自动读取事件，不依赖 Overwolf 或远程服务。
